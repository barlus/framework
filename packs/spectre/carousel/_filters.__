import {$,list,nest,stylesheet} from "@barlus/restyle"

export default Theme;
export const enum Theme {
    filter='filter',
    filterBody='filter-body',
    filterNav='filter-nav',
}
declare module "@barlus/restyle" {
    interface Context {
        filterNumber:any,
    }
}

$.filterNumber = 8;

stylesheet('/Users/Sergey/Work/EXP/spectre/scss/_filters.ts')('',{
PlaceholderPlaceholder    ...nest([`.${Theme.filter}`],{
        ...nest([`.${Theme.filterNav}`],{
            margin:list($.layoutSpacing,0),
        }),
        ...nest([`.${Theme.filterBody}`],{
            display:'flex',
            flexWrap:'wrap',
        }),
    }),
});
